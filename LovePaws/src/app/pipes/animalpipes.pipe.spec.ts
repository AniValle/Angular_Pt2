import { AnimalpipesPipe } from './animalpipes.pipe';

describe('AnimalpipesPipe', () => {
  it('create an instance', () => {
    const pipe = new AnimalpipesPipe();
    expect(pipe).toBeTruthy();
  });
});
